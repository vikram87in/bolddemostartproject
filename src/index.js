import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class DemoClass {
    constructor(props) {
        super(props);
        this.state = {
            
        };
    }
    
    render() {
       
        return (            
            <div>
            </div>
        );
    }
}
DemoClass.defaultProps = {
    
};
DemoClass.propTypes = {
    
};